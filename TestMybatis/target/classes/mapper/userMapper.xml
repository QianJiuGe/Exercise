<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.mapper.UserMapper">
    <select id="getAllUser" resultType="org.example.pojo.User">
        SELECT * FROM t_user;
    </select>

<!--    <insert id="insert" parameterType="org.example.pojo.User">-->
<!--        INSERT INTO student (name,score,age,gender) VALUES (#{name},#{score},#{age},#{gender});-->
<!--    </insert>-->

<!--    <delete id="delete" parameterType="int">-->
<!--        DELETE FROM student WHERE id = #{id};-->
<!--    </delete>-->

<!--    <select id="findByName" parameterType="string" resultType="org.example.pojo.User">-->
<!--        SELECT * FROM student WHERE name like '%${value}%';-->
<!--    </select>-->
</mapper>

